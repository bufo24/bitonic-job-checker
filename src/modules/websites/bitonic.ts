import { JobInput } from ".";

const bitonicInput: JobInput = {
  jobUrl: "https://bitonic.nl/jobs",
  selector:
    ".jobs__list.article-list > article > a > h3.article-list__article-title",
};

export default bitonicInput;
